plugins {
    id 'java-library'
}

group 'guru.qa'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(
            "com.codeborne:selenide:6.4.0",
            "org.junit.jupiter:junit-jupiter:5.8.2"
    )
}

tasks.withType(Test) {
    useJUnitPlatform()
}